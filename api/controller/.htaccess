# Ajoutez cette ligne au début de votre .htaccess pour vérifier

<IfModule mod_headers.c>
    # Supprime les headers existants pour éviter la duplication


    # Ajoute les nouveaux headers


</IfModule>

# Votre redirection HTTPS
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=307,L]